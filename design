module counter (
    input clk,
    input reset,
    input enable,
    output reg [3:0] count
);

always @(posedge clk) begin
    if (reset)
        count <= 4'b0000;
    else if (enable == 1 && count == 4'b1001)
        count <= count + 1;
    else
        count <= 4'b0000;
end
endmodule
